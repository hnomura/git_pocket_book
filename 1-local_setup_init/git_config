git config [--global | --system | --local] name [value]

    == List === 
    > git config --global -l 
    > git config --system -l
    > git config --local -l 


    == Edit == 
    > git config --global -e 

    == Common Properties == 
    > git config --global user.name "Hiroshi Nomura" 
    > git config --global user.email "hnomura.babou@gmail.com"
    > git config --global color.ui auto 

